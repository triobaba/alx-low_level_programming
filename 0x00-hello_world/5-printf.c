#include <stdio.h>
/**
 * main - this is the part of the program where compilation starts,the main f.u
 *
 * Return: by default zero except it in a case where an erroe was encountered.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art\n");
	return (0);
}
